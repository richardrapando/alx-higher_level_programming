#!/usr/bin/python3
def raise_exception_msg(message=""):
    """raises name exception with message
    Args:
        message (string): message to raise with name exception
    """
    raise NameError(message)
